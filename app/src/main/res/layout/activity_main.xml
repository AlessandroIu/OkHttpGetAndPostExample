<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="com.example.alessandro.okhttpgetandpostexample.MainActivity">

    <EditText
        android:id="@+id/get_et"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="4dp"
        android:padding="4dp"
        android:hint="put here the url from where get contents" />

    <Button
        android:id="@+id/get_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="4dp"
        android:padding="4dp"
        android:text="Make a get request on the address above" />

    <Button
        android:id="@+id/set_okhttp_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="4dp"
        android:padding="4dp"
        android:text="Set the text to the OkHttp README.md address" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="120dp"
        android:layout_margin="4dp"
        android:padding="4dp"
        android:background="#5560bf6a">

        <TextView
            android:id="@+id/get_tv"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:hint="Here you will see the contents of the url on which the get method of OkHttp has been performed"
            android:textColor="@android:color/black">

        </TextView>
    </ScrollView>

</LinearLayout>